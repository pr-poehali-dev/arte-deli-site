{
  "tests": [
    {
      "name": "GET /admin/dashboard unauthorized",
      "method": "GET",
      "path": "/",
      "expectedStatus": 403,
      "expectedBody": {"error": "Нет доступа. Требуется роль сотрудника."},
      "bodyMatcher": "partial"
    }
  ]
}